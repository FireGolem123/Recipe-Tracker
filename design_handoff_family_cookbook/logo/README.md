# Logo candidates — NOT YET DECIDED

Two shortlisted app marks. Both are the same pan drawing (top-down skillet, tapered handle,
rivet, hang hole, rotated -30° so the handle points up-right) on different tiles. Decide once
the UI is built out and can be seen with each.

| File | Candidate | Tile | Pan wall / handle | Cooking surface |
|---|---|---|---|---|
| `logo-candidate-9a-cream-tile.svg` / `.png` | 9a | `#F2E8D9` oat cream | `#2E2621` ink | `#B4552F` terracotta |
| `logo-candidate-9d-gradient-tile.svg` / `.png` | 9d | `#C4612F → #B4552F → #9A4626` (150°) | `#2E2621` ink | `#F7E4CE` pale cream |

SVG is the source; PNGs are 512×512 previews. Tile corner radius is 116/512 (≈22.7%) to match
the iOS squircle — regenerate at whatever radius the platform needs.

Notes:
- 9a is the quiet one; it sits back on a busy home screen. 9d is warmer and louder.
- 9a's hang hole is a knockout to the tile colour, so it only works on that exact cream.
  9d drops the hang hole (it closes up at small sizes against the gradient).
- Both hold up to 20px. Below that, drop the rivet dot.
- Rejected along the way, in the prototype for reference: 5a–5c (first pass), 6a–6d
  (side-profile pans), 7a–7c (top-down refinements), 8a–8c (rotation studies),
  9b (terracotta tile), 9c (ink tile).
