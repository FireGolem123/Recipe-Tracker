# apps/web — safe for the browser, RLS makes the anon key safe to expose
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# supabase/functions — edge function env only, never sent to the client
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
